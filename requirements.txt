# ---- Core ----
python-dotenv

# ---- LangChain + LangGraph (this workshop) ----
langchain
langchain-openai
langgraph
langgraph-cli[inmem]>=0.2   # `langgraph dev` + LangGraph Studio (older 0.1.x lacks `dev`)

# ---- Serving deps (LangGraph dev server) ----
fastapi
uvicorn

# ---- Hands-on notebooks ----
jupyterlab
ipykernel
